{
  "name": "dicescripts",
  "version": "0.1.14",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "description": "A library for parsing and evaluating dice expressions.",
  "license": "GPL-3.0-or-later",
  "author": "Arimura Sena <me@hotaru.icu>",
  "repository": {
    "type": "git",
    "url": "https://github.com/biyuehu/dicescript"
  },
  "keywords": [
    "dice",
    "expression",
    "parser",
    "evaluator",
    "roll",
    "dice-roll",
    "roll-dice",
    "idris"
  ],
  "scripts": {
    "start": "node repl/build/exec/Repl",
    "dev": "python scripts/watch.py --root ./ --ext .idr --cmd \"npm run dev:only\"",
    "dev:only": "npm run build && npm run start",
    "build": "cd repl && idris2 --install ../dice.ipkg && idris2 --cg node --build",
    "dev:web": "python scripts/webdev.py",
    "build:web-only": "cd web && idris2 --install ../dice.ipkg && idris2 --cg javascript --build",
    "build:web": "npm run build:web-only && cd web && npx vite build",
    "test": "cd test && idris2 --install ../dice.ipkg && idris2 --cg node --build && node build/exec/Test.js",
    "packs": "python scripts/pack.py"
  },
  "files": [
    "lib",
    "src",
    "dice.pkg",
    "LICENSE",
    "*.md"
  ]
}